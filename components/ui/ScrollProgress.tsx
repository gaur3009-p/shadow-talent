"use client";

/**
 * ScrollProgress — thin gradient line at top of viewport
 * showing scroll position. Width is updated by LenisProvider.
 */
export function ScrollProgress() {
  return <div id="scroll-progress" />;
}
